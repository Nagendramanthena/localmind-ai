# ── Core Runtime ──────────────────────────────────────────────
fastapi[standard]>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0
httpx>=0.27
python-multipart>=0.0.12

# ── Logging ───────────────────────────────────────────────────
structlog>=24.0

# ── LLM Orchestration ────────────────────────────────────────
langchain>=0.3
langchain-ollama>=0.2
langgraph>=0.2

# ── ML / Embedding / Retrieval ────────────────────────────────
# NOTE: On native Windows with DirectML GPU, replace onnxruntime with:
#   onnxruntime-directml>=1.19
# Do NOT install both in the same environment.
onnxruntime>=1.19
transformers>=4.45
tokenizers>=0.20
faiss-cpu>=1.8
numpy>=1.26

# ── Testing ───────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27
